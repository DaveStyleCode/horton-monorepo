{
  "name": "drhorton",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "format": "biome format . --write",
    "check": "biome check ."
  },
  "devDependencies": {
    "@biomejs/biome": "^1.8.3",
    "turbo": "^2.0.0"
  },
  "pnpm": {
    "overrides": {
      "react": "18.2.0",
      "react-native": "0.74.5"
    }
  }
}
